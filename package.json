{
  "name": "encontrados-co",
  "version": "0.2.1",
  "description": "encontrados.co — conecta a quien rescata a una persona con quien la busca. Reporta y consulta por web y API.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node scripts/seed.js",
    "api-key": "node scripts/api-key.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.1107.0",
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2",
    "multer": "^2.2.0",
    "pg": "^8.12.0",
    "sharp": "^0.35.3"
  },
  "license": "MIT"
}
